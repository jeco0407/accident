# Guardy 開發歷程

這份文件記錄 Guardy 從無到有的過程 —— 不只是「做了什麼」，更重要的是**為什麼這樣做**，以及**哪些做法被推翻、為什麼**。

> **維護方式**：每次改版後更新「版本紀錄」，若涉及設計方向的轉折則補進「重大決策」或「被推翻的方向」。這份文件的價值在於保留判斷過程，只列功能清單的話 git log 就夠了。

---

## 目錄

- [專案概要](#專案概要)
- [重大決策](#重大決策)
- [尚未實作的重大轉向](#尚未實作的重大轉向)
- [被推翻的方向](#被推翻的方向)
- [踩過的坑](#踩過的坑)
- [版本紀錄](#版本紀錄)
- [不變的原則](#不變的原則)

---

## 專案概要

**Guardy 是給台灣車禍當事人用的離線優先 PWA。**

車禍發生的當下，人是慌的。要記得報警、要拍照存證、要記對方車牌、要在五日內通知保險公司 —— 每一項漏掉都可能影響後續理賠。Guardy 把這些事排成一條線，一步一步帶著走。

| 項目 | 內容 |
|---|---|
| 形態 | PWA（可加入主畫面、離線可用） |
| 技術 | 純 HTML / CSS / JS，**無框架、無 build step、無後端** |
| 語言 | 繁體中文（台灣） |
| 部署 | Vercel 靜態託管 |
| 原始碼 | 單一 `index.html`（CSS 與 JS 全部內嵌）+ `sw.js` + `privacy.html` |
| 資料 | 全部存在使用者手機本機，不上傳；以「事故案件」為單位分開存放 |

### 五個分頁

1. **現場** —— 定位、110／119、引導模式、現場處理流程
2. **存證** —— 照片清單（真實相機拍攝）、對方資料（車牌／姓名／電話／保險公司）
3. **出險** —— 十家產險公司專線、撥打前準備、後續流程
4. **求償** —— 損失試算、精神慰撫金說明、調解途徑與應備文件
5. **我的** —— 事故案件（切換／結案／刪除）、外觀設定、儲存空間、隱私權政策

---

## 重大決策

### 1. 為什麼不用框架

車禍現場常常沒有訊號。**核心功能必須完全離線可用**是需求規格的第一原則，這個約束往下推導出所有技術選擇：

- 沒有 build step → 沒有 bundle、沒有 hydration、沒有等待
- 單一 HTML 檔 → service worker 快取一個檔案就等於快取整個 App
- 沒有後端 → 沒有「伺服器掛了 App 就不能用」的可能

代價是 `index.html` 長到八萬多字元。這是刻意接受的權衡：**這個 App 的失敗成本是「有人在事故現場打不開它」**，而不是「維護時要多捲幾行」。

### 2. 從一頁式網站改成分頁式 App

v1 完成後的直接回饋是「很不像 App，比較像一頁式網站」。

改成**底部分頁列**（bottom tab bar），配合：

- 固定的 appbar 與 tabbar，用 `backdrop-filter` 做毛玻璃
- 各分頁用 opacity／visibility 疊放而非銷毀重建 —— **保留每個分頁各自的捲動位置**，這是「像 App」最關鍵的一個細節
- hash 路由，讓 Android 實體返回鍵行為正確
- `env(safe-area-inset-*)` 處理瀏海與手勢列

### 3. 圖示全部自己畫

改成日系簡約線條風格，27 個 inline SVG `<symbol>`，用 `<use>` 引用、`currentColor` 上色、`width:1em` 隨字級縮放。

小尺寸下的辨識度是反覆修的重點：

| 問題 | 修正 |
|---|---|
| 車子、拖車太胖 | 重畫車身比例 |
| 三角錐被看成字母 A | 改成錐體 + 底座，拿掉橫桿 |
| 爆炸圖示被看成太陽 | 改成向內收束的箭頭 |
| 盾牌拿來代表警察太籠統 | 新增專用的警示燈圖示 |

### 4. 定位：事故地點與目前位置必須分開

**這是整個專案裡最重要的一個修正。**

原本 `watchPosition` 每次回報都直接覆寫座標。實際情境是：使用者在現場定位（A 點）→ 移車到路邊、走進超商等對方（B 點）→ **A 點永遠消失了**。而且位置是全 App 唯一沒有存檔的資料，關掉再開就回到「尚未定位」。

事故發生的**地點與時間**是報警、出險、跟對方協調最基本的兩個欄位。這不是優化，是缺陷。

改成：

- 第一次定位就鎖成「事故地點 + 時間」寫進 localStorage，開啟時復原
- 目前位置獨立顯示，**距離超過 40 公尺才出現**（GPS 本來就有數公尺誤差，站在原地跳距離只是雜訊）
- 要換地點得按「改設為事故地點」並確認

### 5. 地址用結構化欄位組，不要反轉 display_name

原本是把 Nominatim 的 `display_name` 前五段反轉。實測台北 101 得到：

```
信義區西村里市府路45Devialet
```

`Devialet` 是路邊一家店的店名，而且「號」不見了。

改成用結構化欄位照中文地址順序組：**縣市 → 鄉鎮市區 → 路 → 號**。台灣的欄位對應很反直覺：

| 欄位 | 直轄市 | 縣 |
|---|---|---|
| `suburb` | **區**（中正區） | — |
| `town` | — | **鄉鎮**（礁溪鄉） |
| `city_district` | 里（黎明里） | 村（春陽村） |
| `village` | 商圈（台北站前商圈） | — |

`city_district` 兩種情況都是雜訊，只當最後退路。八個地點實測全部正確：

```
台北車站   → 臺北市中正區北平西路3號
台北101    → 臺北市信義區市府路45號
宜蘭礁溪   → 宜蘭縣礁溪鄉中山路二段
雲林虎尾   → 雲林縣虎尾鎮長春路31巷41號
```

附帶好處：事故地點固定不動，地址一輩子只查一次，Nominatim 請求量從「每 15 秒一次」降到「每個事故一次」，遠低於它每秒一次的使用政策上限。

### 6. 照片存 IndexedDB，不存 localStorage

照片是 blob，localStorage 只能存字串且容量小。存進 IndexedDB 前先處理：

- `createImageBitmap` 解碼（失敗才退回 `<img>`）
- 單次解碼同時產出全圖（長邊 2048、JPEG 0.85）與縮圖（160px）
- 原圖動輒 4MB，縮完約 300–500KB，**仍足以辨識車牌**
- 存 `ArrayBuffer` 而非 `Blob` —— Safari 有 Blob 存進 IndexedDB 的已知問題

第一次存照片時呼叫 `navigator.storage.persist()`，降低瀏覽器清空儲存的機率。

### 7. 深色模式：語意化 CSS 變數 + 繪製前套用

淺深兩色的差異全部收在 CSS 變數裡，元件本身不寫死顏色。

判斷主題的腳本**刻意放在 `<style>` 之後、`</head>` 之前** —— 必須在首次繪製前執行，否則深色模式使用者會看到一閃的白畫面。

### 8. 品牌：Guardy

頂欄的 logo 標記做了去背處理。原圖的底是米白（`#FCF8F3`），在淺色模式下看不出來，**但深色模式會變成一塊突兀的米白方塊**。

用邊界泛洪把**外圍**的米白去成透明，同時保留盾牌**內部**的米白 —— 車身和定位圖釘本來就是米白色，那是設計的一部分。

---

## 尚未實作的重大轉向

### 改為帳號制（設計完成，程式未動）

決定從「純本機、無後端」轉為**強制註冊 + Supabase 後端 + 雲端同步**，一次解決綁定業務員、跨裝置同步、收費與權限三件事。**照片維持不上雲。**

關鍵論證解掉了原本「登入頁會擋住事故現場」的疑慮：

> 使用者不可能在沒有網路的情況下「下載」這個 App。能裝起來，當下就有網路可以註冊。

所以註冊必然發生在有網路時，真正要防的是「已註冊者在離線時被登出」。

完整設計見 [ARCHITECTURE.md](ARCHITECTURE.md)，其中第二節列了五條不可協商的離線鐵則。這個轉向會讓 `privacy.html` 現有的「沒有伺服器、沒有帳號、不蒐集任何個人資料」變成不實陳述，必須整份重寫。

---

## 被推翻的方向

保留這一段，是因為「為什麼不做」往往比「做了什麼」更值錢。

### 邀請碼自帶業務員資料

**構想**：每個保險業務員有自己的邀請碼，客戶輸入後就把該業務員的姓名、電話、LINE 套進 App。客戶出事時能直接聯繫到自己的業務員。

**做法**：邀請碼「自帶資料」—— 把業務員資訊編碼進代碼本身，不需要後端。甚至完整實作了一套純 JS 的 QR 產生器（GF(256) 運算、Reed–Solomon 糾錯、遮罩、格式資訊 BCH），並用 Python 的 `qrcode` 套件逐格比對驗證，72 個固定遮罩矩陣完全一致、產出的 PNG 被 OpenCV 解碼位元組相同。

**為什麼推翻**：關鍵洞察是**「存代碼，不要存資料」**。

自帶資料的代碼會把業務員資訊**永遠凍結**在客戶手機裡。業務員換電話、換公司、離職，客戶手上那份資料就永遠是錯的 —— 而且沒有任何方法更新。中央清單雖然需要後端，但資料的擁有權留在正確的人手上。

這個功能最後整個延後，等多租戶架構確定再說。程式碼裡留下 `who()` 這個函式當作唯一的接縫，未來要接回來只需要改它，UI 不必動。

> 那套 QR 產生器仍然驗證通過、可以用，只是目前沒有用武之地。

### 匯出事故摘要

曾提出把時間、地點、對方資料、照片打包成一則可直接傳送的訊息。**評估後決定不做** —— 現有的「分享位置」與「分享所有照片」已能滿足實際需求，再包一層反而增加維護面積。

### 四家沒有查證電話的保險公司

泰安、旺旺友聯、第一、華南這四家因為拿不到確認過的出險專線，**直接從清單移除，而不是留假號碼佔位**。在一個給事故當事人用的 App 裡，錯誤的電話比沒有電話更糟。

---

## 踩過的坑

### headless Chrome 的虛擬時鐘會製造假的失敗

照片處理流程一度看起來卡在「處理中…」。追查後發現是測試環境的問題：headless Chrome 的 `--virtual-time-budget` 會快轉 `setTimeout`，但真實的圖片解碼 I/O 根本還沒完成 —— **這是測試假象，不是產品 bug**。

為此寫了一支用真實牆鐘時間驅動的 CDP 測試工具，後續所有非同步行為都靠它驗證。

過程中順手做出的真改善：transaction 與請求必須在同一個 tick 內發出（跨 `.then` 會讓 transaction 失效）、一律等 `oncomplete` 才算成功、改存 ArrayBuffer、單次解碼。

### service worker 把新頁面吃掉

早期的 SW 對**任何**導覽請求都回傳 `index.html`。這代表日後新增的任何頁面（例如後來的 `privacy.html`）都會被吃掉。

改成：先找該網址自己的快取 → 沒有才走網路 → 網路也失敗才回落到 `index.html`。

### 測試埠被舊 SW 佔住

SW 的 scope 包含 port，同一個 port 重複測試會拿到上一版的快取，造成「明明改了卻沒生效」的假象。解法是每次驗證換一個乾淨的 port。

### 純 JS QR 產生器的兩個 bug

用 Python `qrcode` 逐格比對才抓出來：

1. **交錯時掉了一個碼字** —— 較短的區塊需要補一個 padding byte 讓所有區塊等長，交錯時再跳過
2. **格式資訊座標錯位** —— `set(6,8,...)` 應為 `set(7,8,...)`，這同時導致 `(8,8)` 沒有被標記為功能模組，所有資料位置整個偏移

### 智慧型手機的 `window.open`

iOS 的 standalone PWA 常常擋掉 `window.open`，「開啟地圖」會沒反應。改用臨時建立的 `<a target="_blank">` 由瀏覽器自己處理，行為一致得多。

### maskable 圖示的接縫

直接縮放整張 logo，會因為原圖左下角的波浪留下一條硬邊。改成用顏色距離門檻找出盾牌的邊界框，再把那個區域置中放到 72%。

---

## 版本紀錄

版號指的是 `sw.js` 裡的 `VERSION`。**改任何檔案都必須把它加一**，否則 cache-first 的舊快取不會被清掉，已安裝的使用者會永遠停在舊版 —— 這是唯一一個漏掉就會出事的步驟。

### v32 — 現場頁依設計稿改版

依使用者提供的設計稿重做「現場」分頁，範圍到引導模式為止。**版型照做，但三處刻意沒照做**：

| 設計稿 | 實際做法 | 為什麼 |
|---|---|---|
| 綠松色系 | 維持暖赤陶 | 配色是全域的，只改一頁會讓切分頁時顏色跳掉，看起來像兩個 App |
| 「AI 引導模式」 | 「引導模式」 | **這個 App 沒有 AI**。引導模式是三個固定問題產生待辦清單。不編造是這個產品的一致原則，標 AI 就是不實陳述 |
| 右上角鈴鐺（紅點） | 齒輪，跳到「我的」 | 沒有通知系統。放一個點了沒反應的鈴鐺，比沒有還糟 |

改動內容：

- **頁首插圖**：inline SVG 的城市天際線 + 車 + 警示三角，用品牌色的低透明度。`pointer-events:none`、文字 `z-index` 疊在上面，窄於 360px 直接收掉 —— 插圖絕不能影響可讀性
- **位置卡**：圓形圖示徽章 + 標題進到卡片裡，加上「尚未取得位置」狀態行與說明
- **110／119**：提示文字從兩張卡下方移進卡片內。原本掛在下面，讀的人得自己把它跟上面連起來
- **道路救援**：改成列表列的語彙（圖示＋標題＋箭頭）。**沒有查證過的號碼仍然不顯示**，`CONFIG.towTel` 留空即隱藏
- **引導模式**：圖示徽章 + 三個問題氣泡 + 實心按鈕。氣泡裡放的是它**實際會問的三題**（有人受傷嗎？車輛可以移動嗎？報警了嗎？），不是舉例

無障礙稽核重跑：**現場頁對比、觸控面積、可讀名稱全部零問題**，齒輪鍵 44×44。

### v31 — 刪光之後要看起來像刪光了

回報：**按了刪除，案件還是存在。**

先追了一輪：刪非目前的、刪目前的、刪最後一件，三條路徑每一步同時比對畫面、記憶體索引與 localStorage 實際內容，而且各自重新載入再驗一次 —— 全部正確，沒有孤兒資料，沒有例外。重現不出來。

問下去才知道**那不是 bug，是我的設計造成的誤會**：刪掉最後一件之後，列表裡留下一列「未記錄地點」。

App 內部確實需要隨時握著一個案件（所有分頁都要有東西可以讀寫），但**那是實作需求，不該讓使用者在畫面上看到**。從使用者的角度，刪完之後列表裡還有一列，就是沒刪掉。

改成：只剩一件而且是空的 → 顯示明確的空白狀態，不畫任何一列，也不顯示結案／新建按鈕。

```
目前沒有事故紀錄
在「現場」取得位置，或到「存證」拍第一張照片，
這裡就會自動出現一件。
```

判斷「空」用的是**所有欄位都空**，而不是「沒有 key」—— 清除進度會留下一堆 `{}`，那跟從沒填過是同一回事。

`go()` 在進入「我的」時重畫一次案件列表。案件標題與空不空都是從別的分頁填出來的，集中在這裡處理，比在每個寫入點都記得通知要可靠。

> 這件事的教訓不在程式碼：**重現不出來的時候，先確認使用者看到的是不是跟我想的同一件事**，而不是繼續往下挖。多問一句就省掉一輪白工。

### v30 — 不再依賴瀏覽器的 confirm()，刪除也不再卡在 IndexedDB

回報：**按了刪除，案件沒有從畫面上消失。**

在 headless 用真實對話框重現不出來（兩列變一列、無任何例外），所以沒有直接的錯誤可以修。改成檢查「有哪些地方本來就可能無聲失敗」，結果有兩處，症狀都完全吻合：

#### 一、`confirm()` 在 PWA 殼裡可能被吃掉

`confirm()` 在 TWA／standalone 這類殼裡有機會**直接回傳 false 而且什麼都不顯示**。使用者按了刪除，看不到對話框，也沒有任何反應 —— 而且查不出原因，因為程式的角度看就是「使用者按了取消」。

改成 App 自己畫的確認對話框：紅色的「刪除」、灰色的「取消」、點背景等同取消、Esc 只關掉它而不會一路關掉底下的東西。結案、建立新案件、清除進度也一併換掉。

順帶好處是文案終於能排版，不必再靠 `\n\n` 硬擠。

#### 二、畫面上的刪除不該取決於一個 IndexedDB 操作

原本的順序是「先清照片，成功了才把案件移出列表」，理由寫在註解裡：反過來會留下沒有案件認領的照片。

這個取捨是錯的。它讓**使用者看得到的結果**取決於一個可能永遠不回來的非同步操作 —— IndexedDB 在某些裝置與情境下就是會卡住不 resolve，此時沒有錯誤、沒有 reject，畫面就只是不動。

翻過來：先改索引、先重畫，照片清理丟到背景。孤兒照片改用開啟時的 `sweepOrphanPhotos()` 掃掉。

> 掃描刻意只刪「明確指向一個已不存在案件」的照片。**沒有 `case` 欄位的一律留著** —— 那可能是升級回填還沒跑完，寧可留下垃圾也不能誤刪使用者的事故照片。

#### 測試腳本的兩個坑

1. **不能覆寫 `window.confirm` 就當作測過了。** 覆寫掉等於跳過真實環境才會出事的部分 —— 這次的 bug 正好就藏在那裡。改成用 CDP 接真實對話框（後來又改成點 App 自己的確認鈕）。
2. **`confirm()` 會凍住 renderer，`Runtime.evaluate` 的回應要等對話框被處理才回來。** 用 `await` 等它就是死鎖，必須 fire-and-forget 再去處理對話框。

還有一個純粹浪費時間的：`python3 -u script.py | tail` 看起來像程式沒有輸出，其實是 `tail` 把輸出憋到程式結束。診斷卡住的程式時不要接 `tail`。

### v29 — 每一件都能刪，包含唯一的那一件

v28 把目前案件的刪除鈕拿掉，理由是「刪掉之後『現在是哪一件』會變成沒有答案」。使用者回報：只有一件的時候就完全刪不掉。

這個限制**是實作偷懶的結果，不是產品該有的規則**。使用者想清空重來，App 不該因為自己的狀態管理處理不了而擋著。

補上真正的處理：

| 情況 | 行為 |
|---|---|
| 刪的不是目前這件 | 從列表移除即可 |
| 刪的是目前這件、還有別的 | 目前案件移到列表第一件，畫面整個重載 |
| 刪的是唯一一件 | 留一件空白的新紀錄 —— App 不能處在「沒有任何案件」的狀態 |

最後一種的確認文案會多一句「這是唯一一件，刪除後會留下一件空白的新紀錄」，讓使用者知道會發生什麼。

實作上有個容易寫錯的地方：`newCase()` 開頭會先 `writeCase()` 把目前案件回寫，但**目前案件剛被刪掉時不能回寫，否則它會長回來**。多一個參數擋掉。

三條路徑都補進 `tests/test_cases.py`，並驗證刪完不留孤兒資料、重新載入狀態正常。

### v28 — 案件切換改成單一列表

v27 的做法是「上方一張『目前案件』卡 + 下方『其他案件』列表」。回饋是**點了之後看不出有切換**。

原因不在缺少提示，而在**變化發生的位置**：使用者正在看下方的列表，切換後更新的卻是上方那張卡 —— 在視線之外，等於什麼都沒發生。加動畫或加提示都只是繞過問題。

改成全部案件排成一張列表，目前這件就標在列表裡：

- 左側圓形勾選標記，選中的填色
- 左緣一條 `--amber` 色條，不必讀文字就知道在哪一件
- meta 後面補「・正在使用」
- toast 改成**說出切到哪一件**（「已切換到：台北市信義區市府路45號」）。只說「已切換」等於沒說，使用者要確認的正是這個

順便：目前這件不提供刪除按鈕。刪掉之後「現在是哪一件」會變成沒有答案，多一段沒必要的邏輯。

### v27 — 事故案件：一次事故一份紀錄

ROADMAP 2.1，長期以來最大的結構性缺口。在此之前所有資料是**全域單一份**：一組地點、一組對方資料、一組照片。處理完下一次事故只能手動清空，清掉就沒了。但理賠往往拖好幾個月，中間再出一次事故完全可能。

#### 儲存方式：一個案件一包 JSON

兩個選項：把每個 key 都加上案件前綴（`aa.<案件>.timeline.v1`），或把整個案件存成一包。

選後者，理由不是資料結構比較漂亮，是**改動面積**：`load()` 與 `save()` 在程式裡有三十幾處呼叫端。只要把這兩個函式改成讀寫「目前案件」那一包，呼叫端一行都不用動。

```js
function load(k){ return GLOBAL_KEYS[k] ? rawLoad(k) : (caseData[k] || {}); }
function save(k,v){ if (GLOBAL_KEYS[k]){ rawSave(k,v); return; } caseData[k]=v; writeCase(); }
```

`aa.cases.v1` 只放索引（`{ cur, list:[{id,ts,closed}] }`），案件內容各自存在 `aa.case.<id>`。標題不存，每次從該案件的位置算出來 —— 存了就會跟事後才查到的地址不同步。

#### 遷移：只搬不刪

舊使用者第一次開新版時，把散在八個 key 的資料收成第一個案件。**刻意不刪舊 key** —— 萬一新版有問題需要退版，舊資料原封不動還在。多佔幾 KB，換一條退路。

#### 照片：IndexedDB 升到 v2

照片紀錄加 `case` 欄位與索引。兩個坑：

1. **建好索引還得逐筆回填**。既有紀錄沒有 `case` 屬性，不會出現在索引裡 —— 少了回填，舊使用者的照片會全部查不到。
2. **查詢就要按案件篩，不能全載進記憶體**。PHOTO_CACHE 存的是 ArrayBuffer，一張 300–500KB，三個案件就是十幾 MB，舊手機撐不住。改用 `index('case').getAll(id)`。

升版時「既有照片歸給哪個案件」要在開啟 DB 前就定下來，不能在非同步的升級交易裡讀 `cases.cur` —— 中途使用者可能已經切換案件了。

#### 切換案件時差點踩到的地雷

`resetLoc()` 會清畫面**並且**寫入空的位置。切換案件時若直接呼叫它，會把剛切過去那個案件的位置**寫成空的** —— 資料就這樣沒了。拆成 `clearLocUI()`（只收畫面）與 `resetLoc()`（收畫面 + 清存檔）。

順帶清掉隱藏 DOM 裡殘留的地址文字：卡片雖然收起來，文字還在。下一件萬一定位失敗，會閃出上一件的地址，這種錯最誤導人。

#### 語意調整

「清除所有進度」「刪除所有照片」「分享所有照片」的「所有」現在都只指**目前這件**，文案一併改掉。清除進度加了確認對話框，並指出「若要留著紀錄，請改用結案」。

#### 測試

`tests/test_cases.py`：模擬 v26 使用者（八個舊 key + 舊 schema 的照片），驗證遷移、案件隔離、切換後資料回復、重新載入記得目前案件、結案、刪除案件時照片一起走。

寫測試時自己踩到一個坑：在 `index.html` 上執行 `deleteDatabase` 會 `onblocked` 而不是 `onsuccess`，因為頁面本身開著連線 —— 測試直接卡死。改成在同源但不載入 App 的頁面（`manifest.json`）上種資料。

### v26 — 無障礙：對比與觸控面積

ROADMAP 3.3 從頭到尾沒做過系統性檢查。寫了一支稽核腳本，用 `getComputedStyle` 掃過五個分頁所有可見的葉節點，實際計算對比度與觸控面積 —— **不是憑感覺看**。

#### 對比

抓到 25 處低於 WCAG AA 的 4.5:1，根源是兩個 token：

| token | 原值 | 對比 | 新值 | 對比 |
|---|---|---|---|---|
| `--muted` | `#8C8177` | 3.07:1 | `#6E665D` | 4.56:1 |
| `--amber` | `#B0700C` | 3.28:1 | `#9F650B` | 4.50:1 |
| 箭頭（用 `--line`） | `#EDE6DC` | **1.00:1** | 新增 `--chev` | 3.07:1 |

箭頭那條最誇張 —— `1.00:1` 表示它跟背景**完全同色**，等於看不見。原本直接借用了邊框色。

新值是**演算出來的**：保持相同色相與飽和度，只逐步降低明度直到最差背景組合也達標，所以視覺調性沒變。深色模式另外算一組。

#### 觸控面積

五處小於 44×44：重新定位、關閉、重設、拍照鍵、勾選圈。

前四個直接加 `min-height`。**勾選圈不能這樣做** —— 它視覺上就是一個 24px 的小方框，加 padding 會讓外框跟著撐大而變形。改用偽元素擴張點擊區：

```css
.ph .box::after{content:'';position:absolute;inset:-10px}
```

用 `elementFromPoint` 在方框外側取樣驗證：外側 8px 命中、14px 落空，視覺尺寸維持 27×24 不變。

#### 稽核腳本自己的 bug

第一版報告求償頁七個輸入框「沒有可讀名稱」，但它們都有 `label[for]` 關聯 —— 是腳本沒檢查這種關聯方式。**修的是腳本，不是產品。** 工具報錯不代表產品有錯。

### v25 — 頁首加上暖色區塊

大標題只有文字，視覺上仍嫌單薄。頁首改為滿版的暖赤陶漸層區塊（用負 margin 抵銷 `.view-in` 的 padding 延伸到螢幕邊緣），底部一條細分隔線。

實作前先渲染了三個版本比較：**A 暖赤陶**、**B 薄荷綠**、**C 圖示色塊徽章**。

選 A 的關鍵是**顏色語意**：這個 App 裡的綠色已經代表「已完成、已取得」—— 定位成功的卡片、勾完的步驟、有憑據的小計都是薄荷綠。版本 B 把頁首也塗成綠色之後，定位卡在畫面上變得不明顯，那個「定位成功」的安心訊號被稀釋掉了。**截圖並排看才發現這件事，光想是想不出來的。**

暖赤陶跟品牌主色一致（CTA 按鈕、選中的分頁），不跟綠色的「完成」或紅色的「緊急」搶語意。

顏色收成 `--head-bg` / `--head-line` 兩個主題變數，深色模式用溫暖的深褐（`#2A1D15`）往背景漸層，不刺眼。

### v24 — 每頁加上大標題頁首

v21 移除頂欄後，每頁最上方只剩一句 16.5px 的淡色說明文字，撐不起版面。而且五頁不一致 —— 求償與我的連那句都沒有。

頂欄原本兼了三件事：品牌、頁面標題、視覺錨點。改用**會跟著內容捲走的大標題**補回來：30px 粗體標題 + 原本的說明句當副標。有份量，但不像固定列那樣永久佔掉 54px。

- 現場：`GUARDY` 字標 + 標記，再接「事故現場」。**只有這一頁放品牌** —— 每頁都放就成了裝飾
- 存證：拍照存證
- 出險：通知保險公司
- 求償：調解與求償（順便補回 v17 拿掉的那句說明，改短）
- 我的：我的（無副標）

深淺色、360px 窄螢幕都驗過，無橫向溢出。

### v23 — 對方資料精簡為四欄

拿掉駕照號碼、車輛、備註，並移除車牌與保險公司下方的說明小字。剩下**車牌、姓名、電話、保險公司**四欄。

現場向對方問資料時能問到的就這幾樣，欄位越少越可能被填完。

渲染器的 `hint` 與 `area` 分支保留 —— 那是資料驅動的通用能力，之後要加回說明或多行欄位只需改 `OTHER_FIELDS`，不必動程式碼。

> 舊版使用者 localStorage 裡殘留的 `license`／`car`／`memo` 會被忽略（讀寫都以 `OTHER_FIELDS` 為準），不影響運作。

### v22 — 修正對方資料表單的幽靈輸入框

實機回報：對方資料的選填欄位下面各多出一個空框。

**是 class 撞名。** 表單用 `<span class="opt">（選填）</span>` 標示選填，但 `.opt` 早已是引導模式那個大選項按鈕的樣式（`min-height:72px`、外框、卡片背景、陰影）。那個「框」不是設計，是繼承來的。

`.f label .opt{font-weight:400;color:var(--muted)}` 只覆蓋了字重與顏色，蓋不掉盒子模型，所以問題一直沒被發現。

處理：

- 拿掉（選填）標記。除了車牌以外全部都是選填，逐欄標示本來就是雜訊
- 移除失效的 `.f label .opt` 規則與 `OTHER_FIELDS` 的 `opt` 旗標
- **把引導模式的 `.opt` 改名為 `.q-opt`**，避免同樣的事再發生一次。`opt` 這種泛用字在單檔 CSS 裡遲早會再撞到

> 這類 bug 光讀程式碼看不出來 —— 兩處各自都合理，只有渲染出來才知道撞在一起。定位這種問題時，先把畫面畫出來比逐行推理快得多。

### v21 — 移除頂欄

拿掉固定在最上方的 appbar（logo + 分頁標題）。每一頁多了 54px 的可視高度，內容直接從最上面開始。

分頁名稱本來就在底部分頁列標示，頂欄再寫一次是重複的。

**離線標示原本住在頂欄裡**，改成只在斷線時浮出的小標籤（右上角，避開瀏海）。這個資訊不能一起拿掉 —— 它是「地圖鍵為什麼變灰、地址為什麼查不到」的唯一解釋。

`--appbar` 變數移除，`--top` 直接等於 `env(safe-area-inset-top)`。`VIEWS` 的標題文字保留當註解，實際只用來驗證分頁名稱是否有效。

### v20 — 修正門牌重複「號」

實機在高雄看到「英明一路72巷20弄16之3號**號**」。

原因是 Nominatim 的 `house_number` 格式不一致：台北回的是純數字 `"3"`，高雄回的是完整門牌 `"115號"`、`"16之3號"`。程式無條件補「號」，遇到後者就重複了。

改成只在結尾不是號／樓／室時才補。單元測試涵蓋 `3`／`16之3號`／`16-3`／`5樓`／空值，四個實際座標也重新驗過。

> 這種只在部分地區才出現的資料格式差異，靠單一地點測試抓不到 —— 台北那組測資從頭到尾都是對的。

### v19 — 位置卡片精簡

定位後只留**一行位置 + 複製／分享／地圖**，再加上「重新定位」與「關閉」兩個次要動作。拿掉的有：事故地點標題、記錄時間、座標列、說明文字、「目前位置距離」那一列。

三個延伸決定：

- **離線時那一行改顯示座標。** 嚴格「只顯示地址」會讓離線使用者看到空白，而唸座標給接線員正是這個 App 最核心的用途。版面仍然只有一行，只是內容從地址換成座標，並附一句說明為什麼。
- **`watchPosition` 整個移除。** 它原本只為了算「離事故地點多遠」，那一列拿掉後就只剩耗電。v11 建立的「背景定位不會覆蓋記錄位置」這個保護仍然成立 —— 現在根本沒有背景定位，只有使用者主動按「重新定位」才會取代。
- **「關閉」會清掉存檔，不只是隱藏。** 只隱藏的話，下次開 App 又會冒出來，那不叫關閉。

`resetLoc()` 同時被「關閉」與「清除所有進度」共用，原本後者自己維護一份一模一樣的收合邏輯。

### v18 — 步驟卡改成兩排，最小字級定為 12px

**步驟卡（現場處理、出險流程共用）改成上下兩排：**上排是圖示 + STEP／標題 + 勾選圈，下排是說明文字**跨滿整個寬度**。

原本說明文字跟標題一起縮在圖示右側，欄寬只剩約七成。步驟說明常常是完整句子，被擠成兩三行讀起來很碎。移到獨立一列後，同樣的文字少斷一行，整張卡也變矮。

兩個 renderer（`renderTL`／`renderCL`）原本各自拼一份一模一樣的 HTML，這次抽成共用的 `tlMarkup()`。

**最小字級統一定為 12px。** v16 調整後最小是 11px（分頁標籤）與 11.5px，這次一併提到 12px；以 `getComputedStyle` 掃過所有葉節點確認實際渲染的最小字級就是 12px，沒有漏網的。

### v17 — 求償頁的準備清單簡化

- 刪掉求償頁的開場白，直接進入內容
- 應備文件清單**拿掉每一項的說明小字，只留品名**，字級從 16px 拉到 19px。這是到現場前的核對清單，掃過去要能一眼看完，說明文字反而讓人讀得慢
- 「雙方保險資料」改成「行照跟駕照」—— 調解現場實際要出示的是這兩張證件

兩個較長的官方文件名（道路交通事故初步分析研判表、醫療費用收據正本與診斷證明書）在 360px 下會折成兩行。**不縮寫**：那是向警方或醫院申請時要講的正式名稱，寫短了反而不好辦事。

### v16 — 全站字級調大

原本最小的說明文字只有 12px，在手機上偏小。這個 App 的使用者可能正在受傷、手在抖、天色昏暗，字太小是實際的可用性問題，不是美感偏好。

全站 107 處字級統一往上調，**小字加得比大字多**（10px → 11px 是 +10%，22px → 23px 只有 +4.5%），這樣既拉高了最小可讀尺寸，又壓縮了過大的層級落差。26px 以上（110／119、金額總計）是展示數字，本來就夠大，維持不動。

順手把 `.amt-box` 從 124px 收到 116px，並加上 380px 以下收到 100px 的斷點，讓求償欄位的說明少斷一行。

以 360px（常見的最窄手機寬度）驗證：無橫向溢出、五個分頁標籤沒有換行。

> 這次改動一度被 IDE 用舊的緩衝區覆寫，連帶刪掉了求償頁的開場白。`git diff --stat` 只顯示 5 行變更才發現。批次改檔後養成看 diff 行數的習慣，能及早抓到這種事。

### v15 — 求償分頁人性化

v14 的求償頁功能是對的，但一打開是七個並排的數字輸入框，讀起來像報稅。使用者可能正在受傷、正在焦慮，介面不該再給他壓力。

- **開場改成設定期待**：「這一頁是幾週、甚至幾個月的事。不用今天填完，填多少就存多少。」
- **七個欄位分成兩組**（身體與照護／收入與財物），一次面對四個比面對七個容易
- **$0 的空狀態改掉**。`$0` 看起來像「你什麼都沒有」，實際上只是還沒開始填。改成「還沒有填入金額 —— 手邊有幾張收據就先填幾張」，填了才變綠底並顯示金額與「目前填了 N 項，隨時可以再補」
- **填過的欄位變綠底**，一眼看得出哪些做完了
- **應備文件改成可勾選**，加上「已備妥 3 / 8」進度與重設。這也修掉一個不一致 —— 其他分頁的清單都能勾，只有這裡不行。八項待辦看了讓人卻步，能一項一項打勾就變成看得到盡頭的事；全部勾完會顯示「都準備好了」
- **慰撫金那段重寫**。原本開頭是「沒有公式，也沒有行情表」，讀起來像拒絕回答。改成「你受的苦是真的，只是它沒有價目表」—— 先承認，再解釋為什麼不給數字，並說明給錯數字的實際後果（開太高談不攏、開太低自己吃虧）

過程中一度在頁尾寫了法律扶助基金會的免付費專線，隨即移除 —— 那違反「嚴禁自行生成電話號碼」的原則，即使是很常見的號碼也一樣。

### v14 — 新增「求償」分頁：調解與求償試算

第五個分頁。調解與求償是事發後幾週到幾個月的事，跟現場、出險是不同階段，所以獨立成一頁。

**核心設計是把「算得出來的」跟「算不出來的」嚴格分開：**

- **算得出來的**：醫療、復健、看護、就醫交通、工作損失、車輛修復、其他財物。七個欄位讓使用者填自己**單據上的**金額，App 只負責加總。這份小計的力量在於每一筆都有收據可以佐證
- **算不出來的**：精神慰撫金**不給任何數字**。沒有公式，同樣傷勢判下來可以差好幾倍，任何估算都是誤導 —— 而且使用者拿著錯的數字去調解會直接受害。改為說明法院衡量的六項因素，並提供司法院判決查詢的入口，讓他用**真實判決**當談判基準

另有調解三途徑（鄉鎮市區調解委員會／法院調解／直接訴訟）與八項應備文件。**具體法條編號、時效、強制險上限一律標成【待查證】並用紅底標示**，跟保險公司電話同一個做法：寧可留白，不可編造。

複製出來的求償明細會自動帶入事故時間與地點，並註明「精神慰撫金另計」。

順手把複製邏輯抽成共用的 `copyText()`，原本內嵌在位置的複製按鈕裡。

### v13 — 定位失敗時的手動輸入

原本定位失敗只給一句「請再試一次」，然後就沒有然後了 —— **沒有任何方法記下事故地點**。而定位失敗最常發生的地方（地下停車場、隧道、室內），正好是擦撞事故最常發生的地方之一。這是核心流程的死路，不是體驗優化。

- 定位失敗時**自動展開**手動輸入，不要讓使用者自己去找退路
- `site` 新增「只有地址、沒有座標」的狀態（`manual:true`，`lat`／`lon` 為 `null`）
- 沒有座標時：隱藏座標列、地圖改用地址查詢、分享文字省略座標
- 之後定位得到可以補上，會確認後才覆蓋手動輸入的文字
- 手動輸入用外框樣式，不跟主要的定位按鈕搶視覺重量

實作時測出一個兩步驟的 bug：手動地點按「再試一次定位」，定位成功後 `onPos` 看到 `state.site` 已存在就只重繪，要再按第二次「改用目前定位」才生效。使用者按那顆按鈕的意圖很明確，已改為定位成功就直接詢問。

### v12 — 商店上架的必要條件

- `privacy.html` 隱私權政策（兩邊商店都強制要求公開網址）
- 「我的 → 關於」加入隱私權政策入口
- manifest 補 `id`、`screenshots`、`shortcuts`
- 六張 1080×1920 商店截圖（刻意不放進離線快取）
- 開啟時尊重主畫面捷徑帶進來的 hash，重新整理與上一頁仍回「現場」
- README 補上 Play 與 App Store 的差別評估

### v11 — 保存事故地點與時間

- 事故地點與時間鎖定並存檔，不再被 `watchPosition` 覆蓋
- 目前位置獨立顯示，超過 40 公尺才出現
- 地址改用 Nominatim 結構化欄位組
- 地圖按鈕改用 `<a target="_blank">`

### v10 — 品牌更名為 Guardy

- 頂欄改用實際 logo，做透明去背處理
- 全站品牌名稱統一

### v9 — 新圖示與部署前修正

- 換上正式的 App 圖示（192／512／maskable／apple-touch）
- Nominatim 請求節流

### v8 — 深色模式

- 語意化 CSS 變數、跟隨系統或手動指定
- 繪製前套用主題，避免白畫面閃爍

### v7 — 對方資料與照片存證

- 七欄對方資料表單，輸入即存
- 真實相機拍攝、IndexedDB 儲存、縮圖與檢視器

### v2–v5 — 未獨立成版的階段

這幾個版號用在同一段連續開發裡，沒有各自的提交：**App 內圖示改成日系簡約線條**、**從一頁式網站改為底部分頁架構**、以及**邀請碼系統的完整實作**。

前兩項的設計理由記在[重大決策](#重大決策)的第 2、3 點；邀請碼最後整個被推翻，記在[被推翻的方向](#被推翻的方向)。因為推翻時一併移除，這段程式碼沒有留在任何一次提交裡。

### v6 — 移除業務員聯絡功能

- 邀請碼與業務員區塊整個移除，留 `who()` 當接縫

### v1 — 離線優先的事故處理 PWA

- 四分頁架構、定位、110／119、引導模式、現場流程、照片清單、出險專線

---

## 不變的原則

這些是從第一天就確立、至今沒有動搖的約束：

1. **核心功能必須完全離線可用** —— 現場沒訊號是常態，不是例外
2. **首屏永遠只有：Hero + 位置 + 110／119** —— 慌的時候不該需要捲動
3. **嚴禁自行生成電話號碼** —— 所有號碼由人工逐一查證後填入
4. **不放任何 API key 在前端**
5. **對方資料與照片只存在使用者手機本機，不上傳任何地方**
6. **不編造數據** —— 沒有查證過的統計、法規期限、保險公司流程一律不寫

# Guardy 待辦與規劃

依「必須先解決」到「有餘裕再說」排序。每一項都寫了**為什麼**，避免日後回頭看只剩一句沒有脈絡的功能名稱。

> 完成的項目請移到 [DEVLOG.md](DEVLOG.md) 的版本紀錄，不要留在這裡打勾。

---

## 一、上架前一定要處理

這幾項沒完成就不能對外，跟功能好壞無關。

### 1.1 隱私權政策的聯絡信箱 ✅

已填入 `getnabor@gmail.com`，`privacy.html` 與 `terms.html` 都是同一個。

> 這個地址會被公開在網路上、被爬蟲抓走，而且它同時是隱私權與個資權利行使的窗口 —— 依個資法，查詢、更正、刪除的請求都會寄到這裡。**確認這是一個你願意公開、而且會固定去看的信箱。** 若之後想換成獨立的對外地址（例如 support@ 自己的網域），改這兩個檔案即可。

### 1.2 綁定自己的網域

TWA 的網域驗證**不接受** `*.vercel.app`。這是包 Google Play 版的前提，不是可選項。

### 1.3 Vercel 方案

Hobby 方案**禁止商業用途**。只要這個 App 帶來任何商業利益（包含業務員導流），就必須升級到 Pro。這是條款問題，不是效能問題。

### 1.4 求償分頁的法律內容查證 ⭐

「求償」分頁裡有三處標成【待查證】並用紅底顯示，上線前必須填上或刪除：

| 位置 | 待確認 |
|---|---|
| 鄉鎮市區調解委員會 | 調解書經法院核定後的效力依據條文 |
| 法院調解 | 強制調解事件的範圍與費用 |
| 直接提起民事訴訟 | 侵權行為損害賠償請求權的時效規定 |

搜尋 `COMP_PATHS` 即可找到。**紅底標示是刻意的** —— 沒填完就上線，使用者一眼看得出那是未確認資訊，不會被誤導。

另外目前完全沒有提到強制險的給付範圍與上限。若要補，同樣需要查證後才寫。

### 1.5 出險專線重新查證

十家產險公司的專線目前是人工填入的。號碼會異動，上線前與定期都要重新確認。**打錯電話的代價由使用者承擔**。

### 1.6 App Store 的可行性評估

Apple 審查指南 **4.2 Minimum Functionality** 明文排除「把網站包起來」的 App。純 WKWebView 殼被拒是常態。

建議：**先做 Google Play（TWA 是成熟做法），iOS 先靠「加入主畫面」**。等到有原生功能非做不可時（例如背景定位、緊急聯絡人自動通知）再評估 iOS，不要先付 99 美元年費才發現過不了。

---

## 二、產品本身的缺口

### 2.1 對方資料沒有出口

四個欄位填完之後，資料只躺在 App 裡。實際上這些資料要給業務員、給警察、給對方。目前只能自己一個一個唸出來。

（先前評估過「匯出事故摘要」，當時決定不做。案件概念已於 v27 完成，這件事現在可以自然做成「匯出這個案件」，不必再另外設計容器。）

### 2.2 照片有遺失風險

照片存在 IndexedDB，會隨「清除瀏覽器資料」一起消失。目前的做法是在 App 內提示使用者用「分享」備份到相簿 —— **這是提示，不是保障**。

沒有雲端備份是刻意的隱私選擇，但至少可以做得更好：例如拍完後主動提示存到相簿、或在儲存空間不足時警告。

### 2.3 行車紀錄器影片

出險流程裡提到「交付照片與行車紀錄器」，但 App 完全沒有處理影片。影片檔案大、處理慢，需要單獨評估。

---

## 三、體驗優化

### 3.1 首次開啟的引導

現在打開就是完整介面。沒出過事故的人不知道這個 App 能做什麼，也就不會在真的出事時想到它。

### 3.2 引導模式的問題設計

三個問題產生待辦清單。問題本身是否問對了、產生的清單是否真的有用，需要找真的出過事故的人驗證。

### 3.3 無障礙（部分完成）

**已做**：對比度全面達到 WCAG AA 4.5:1、觸控面積全部 ≥ 44×44、字級最小 12px。稽核腳本在 `tests/a11y.py`，改版後可重跑。

**未做**：螢幕閱讀器（VoiceOver／TalkBack）實機測試、鍵盤操作順序。（`prefers-reduced-motion` 目前只有 v52 歡迎屏那枚脈動的盾有處理，其餘動效還沒盤點。）

### 3.4 登入流程與 App 本體的配色斷層 ✅

**大致解決了。** v52 統一了強調色，v53 讓這四屏跟隨 `data-theme`（跟 App 本體同一個來源，「個人 → 外觀」也管得到）。淺色模式下登入流程與 App 幾乎沒有落差。

剩下的只有深色模式：這四屏的底是中性冷黑 `#151517`，App 本體是偏暖的棕黑 `#16130F`。**這是刻意的**，兩套版型本來就不同，不打算收。若日後覺得礙眼，改 `:root[data-theme="dark"] .auth` 裡的 `--a-bg` 即可。

根目錄的 `圖片.png`（2MB）沒有任何引用，確定不用了就可以刪。

---

## 四、商業層面的未解題

### 4.1 帳號制與多租戶架構 ⭐

**已完成設計，尚未實作。詳見 [ARCHITECTURE.md](ARCHITECTURE.md)。**

決定改為帳號制（強制註冊）+ Supabase 後端 + 雲端同步，一次解決綁定業務員、跨裝置同步、收費與權限三件事。照片維持不上雲。

**M1–M3 已完成**（v36 帳號骨架、v37 Supabase Auth、v39 事故資料同步），仍**預設關閉**，`?auth=1` 可預覽。

打開之前一定要先做這幾件事：
1. 在 Supabase 後台依序執行 `supabase/001_profiles.sql`、`002_incidents.sql`、`003_role.sql`
2. ~~重寫 `privacy.html`~~ **已於 v42 完成。** 開放帳號時要做的是：拿掉兩份文件最上面的「尚未對外開放」狀態框、更新日期、補上 Supabase 資料中心所在地區（目前標【待查證】）
3. **`terms.html` 找律師審閱** —— 責任限制與免責那幾節尤其重要

**M4 業務員綁定與 M5 業務員後台已於 v43 取消**（理由見 [DEVLOG 被推翻的方向](DEVLOG.md#被推翻的方向)）。取而代之的是 v43 的「身分」：登入後自己選一般使用者或保險業務員，**不帶任何資料權限**。目前身分只被記錄下來，業務員專屬功能尚未決定要做什麼。

原本的 M1–M6 因此只剩 M1–M3，且都已完成。

> 最需要小心的是**離線鐵則**：已註冊的使用者絕不能因為離線而被擋在外面。ARCHITECTURE.md 第二節列了五條不可協商的規則。

### 4.2 誰付錢、付多少 ⭐ 待解

**v43 取消業務員綁定之後，目前沒有任何收入來源。**

原本的模式是 B2B2C：業務員付費訂閱、客戶免費使用，訂閱賣在業務員網頁後台而不走 App 內購，藉此避開 Google Play 15–30% 抽成。**那個模式整個建立在綁定上**，綁定沒了，它就不成立了。

避開抽成的做法本身仍然有效，可以留給未來任何付費設計沿用。要賣什麼、賣給誰，待定。

### 4.3 法律責任邊界

App 提供事故處理指引。若使用者照著做卻產生不利結果，責任如何界定？目前只有免責聲明，**建議在有實際使用者之前先諮詢律師**。

---

## 五、技術債

### 5.1 `index.html` 已經八萬多字元

單檔是刻意的選擇（見 DEVLOG），但已經接近可維護的上限。若要再長，需要在**不引入 build step** 的前提下想辦法拆 —— 例如把 SVG sprite 或 INSURERS 資料抽成獨立檔案由 SW 快取。

### 5.2 沒有自動化測試

驗證腳本收在 `tests/`（見該目錄的 README），但仍是**手動重跑**：改一個地方要跑哪幾支，全憑記憶。

### 5.3 沒有錯誤回報

使用者遇到問題，我們完全不會知道。無後端架構下這是天然的限制，但至少可以做「把錯誤訊息複製給我」這種手動回報。

---

## 優先順序建議

| 順位 | 項目 | 理由 |
|---|---|---|
| 1 | 一、上架前必辦（1.1–1.5） | 多為純執行；1.4 需要法律查證 |
| 2 | 重寫 `privacy.html` | M1–M3 已完成，打開帳號功能前這是硬條件 |
| 3 | 2.1 對方資料的出口 | 資料填了卻拿不出來；案件容器已就緒 |
| 4 | 3.1 首次引導 | 影響留存，但要先有使用者 |

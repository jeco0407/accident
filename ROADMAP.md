# Guardy 待辦與規劃

依「必須先解決」到「有餘裕再說」排序。每一項都寫了**為什麼**，避免日後回頭看只剩一句沒有脈絡的功能名稱。

> 完成的項目請移到 [DEVLOG.md](DEVLOG.md) 的版本紀錄，不要留在這裡打勾。

---

## 一、上架前一定要處理

這幾項沒完成就不能對外，跟功能好壞無關。

### 1.1 隱私權政策的聯絡信箱

`privacy.html` 裡還是 `PLACEHOLDER_EMAIL`。兩邊商店都會檢查這個頁面，而且這個信箱會被公開在網路上，建議另開一個對外用的地址，不要用私人常用信箱。

### 1.2 綁定自己的網域

TWA 的網域驗證**不接受** `*.vercel.app`。這是包 Google Play 版的前提，不是可選項。

### 1.3 Vercel 方案

Hobby 方案**禁止商業用途**。只要這個 App 帶來任何商業利益（包含業務員導流），就必須升級到 Pro。這是條款問題，不是效能問題。

### 1.4 求償分頁的法律內容查證 ⭐

「求償」分頁裡有三處標成【待查證】並用紅底顯示，上線前必須填上或刪除：

| 位置 | 待確認 |
|---|---|
| 鄉鎮市區調解委員會 | 調解書經法院核定後的效力依據條文 |
| 法院調解 | 強制調解事件的範圍與費用 |
| 直接提起民事訴訟 | 侵權行為損害賠償請求權的時效規定 |

搜尋 `COMP_PATHS` 即可找到。**紅底標示是刻意的** —— 沒填完就上線，使用者一眼看得出那是未確認資訊，不會被誤導。

另外目前完全沒有提到強制險的給付範圍與上限。若要補，同樣需要查證後才寫。

### 1.5 出險專線重新查證

十家產險公司的專線目前是人工填入的。號碼會異動，上線前與定期都要重新確認。**打錯電話的代價由使用者承擔**。

### 1.6 App Store 的可行性評估

Apple 審查指南 **4.2 Minimum Functionality** 明文排除「把網站包起來」的 App。純 WKWebView 殼被拒是常態。

建議：**先做 Google Play（TWA 是成熟做法），iOS 先靠「加入主畫面」**。等到有原生功能非做不可時（例如背景定位、緊急聯絡人自動通知）再評估 iOS，不要先付 99 美元年費才發現過不了。

---

## 二、產品本身的缺口

### 2.1 事故案件的概念 ⭐

目前所有資料是**全域單一份**：一組事故地點、一組對方資料、一組照片。處理完下一次事故要手動清空，而且清掉就沒了。

真實情況是人會想留著紀錄 —— 理賠往往拖好幾個月，中間可能還會再出一次事故。

需要：可結案、可保留歷史、可切換目前處理中的案件。

> **這是目前最大的結構性缺口，也是最貴的一項**，會動到所有資料的儲存格式。建議在使用者量還小的時候做，遷移成本最低。

### 2.2 對方資料沒有出口

七個欄位填完之後，資料只躺在 App 裡。實際上這些資料要給業務員、給警察、給對方。目前只能自己一個一個唸出來。

（先前評估過「匯出事故摘要」，當時決定不做。若 2.1 的案件概念要做，這件事會自然變成案件的一部分，屆時再一起處理。）

### 2.3 照片有遺失風險

照片存在 IndexedDB，會隨「清除瀏覽器資料」一起消失。目前的做法是在 App 內提示使用者用「分享」備份到相簿 —— **這是提示，不是保障**。

沒有雲端備份是刻意的隱私選擇，但至少可以做得更好：例如拍完後主動提示存到相簿、或在儲存空間不足時警告。

### 2.4 行車紀錄器影片

出險流程裡提到「交付照片與行車紀錄器」，但 App 完全沒有處理影片。影片檔案大、處理慢，需要單獨評估。

---

## 三、體驗優化

### 3.1 首次開啟的引導

現在打開就是完整介面。沒出過事故的人不知道這個 App 能做什麼，也就不會在真的出事時想到它。

### 3.2 引導模式的問題設計

三個問題產生待辦清單。問題本身是否問對了、產生的清單是否真的有用，需要找真的出過事故的人驗證。

### 3.3 無障礙

字級、對比、螢幕閱讀器都還沒有系統性檢查過。緊急情境下的 App，這件事比一般產品更重要。

---

## 四、商業層面的未解題

### 4.1 多租戶架構 ⭐

**這是整個商業模式的核心，目前是空的。**

原始構想是讓每個客戶能聯繫到自己的保險業務員。試過「邀請碼自帶資料」，因為會把業務員資訊永遠凍結在客戶手機裡而推翻（詳見 [DEVLOG.md](DEVLOG.md#被推翻的方向)）。

正確的方向是**中央清單 + 代碼**：客戶存的是代碼，資料留在後端由業務員自己維護。但這意味著：

- 需要後端（目前完全沒有）
- 需要業務員的註冊與管理介面
- 需要處理「後端掛了怎麼辦」—— 不能違反離線優先的第一原則

程式碼裡的 `who()` 函式是預留的接縫，接回來時 UI 不必動。

### 4.2 誰付錢、付多少

多租戶做出來之後才會面對的問題，但**現在就該想清楚**，因為它會決定架構怎麼設計。

### 4.3 法律責任邊界

App 提供事故處理指引。若使用者照著做卻產生不利結果，責任如何界定？目前只有免責聲明，**建議在有實際使用者之前先諮詢律師**。

---

## 五、技術債

### 5.1 `index.html` 已經八萬多字元

單檔是刻意的選擇（見 DEVLOG），但已經接近可維護的上限。若要再長，需要在**不引入 build step** 的前提下想辦法拆 —— 例如把 SVG sprite 或 INSURERS 資料抽成獨立檔案由 SW 快取。

### 5.2 沒有自動化測試

目前靠 CDP 腳本手動驗證關鍵流程。改一個地方要重跑哪些測試，全憑記憶。

### 5.3 沒有錯誤回報

使用者遇到問題，我們完全不會知道。無後端架構下這是天然的限制，但至少可以做「把錯誤訊息複製給我」這種手動回報。

---

## 優先順序建議

| 順位 | 項目 | 理由 |
|---|---|---|
| 1 | 一、上架前必辦（1.1–1.5） | 多為純執行；1.4 需要法律查證 |
| 2 | 2.1 事故案件概念 | 最大結構缺口，**越晚做越貴** |
| 3 | 2.2 對方資料的出口 | 資料填了卻拿不出來 |
| 4 | 4.1 多租戶架構 | 商業模式的前提，但工程量最大 |
| 5 | 3.1 首次引導 | 影響留存，但要先有使用者 |

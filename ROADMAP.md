# Guardy 待辦與規劃

依「必須先解決」到「有餘裕再說」排序。每一項都寫了**為什麼**，避免日後回頭看只剩一句沒有脈絡的功能名稱。

> 完成的項目請移到 [DEVLOG.md](DEVLOG.md) 的版本紀錄，不要留在這裡打勾。

---

## 一、上架前一定要處理

這幾項沒完成就不能對外，跟功能好壞無關。

### 1.1 隱私權政策的聯絡信箱 ✅

已填入 `getnabor@gmail.com`，`privacy.html` 與 `terms.html` 都是同一個。

> 這個地址會被公開在網路上、被爬蟲抓走，而且它同時是隱私權與個資權利行使的窗口 —— 依個資法，查詢、更正、刪除的請求都會寄到這裡。**確認這是一個你願意公開、而且會固定去看的信箱。** 若之後想換成獨立的對外地址（例如 support@ 自己的網域），改這兩個檔案即可。

### 1.2 綁定自己的網域

v58 起不上架，TWA 的網域驗證不再是理由；但客戶收到 `xxx.vercel.app` 這種陌生網址容易當成詐騙連結。**發給客戶之前要綁好。**

### 1.2.1 發給客戶的網址

建議直接帶 `?openExternalBrowser=1`，從 LINE 點開就會跳到系統瀏覽器。「聯絡我們」連到 LINE 官方帳號，換帳號時改 `CONFIG.contactUrl`。

### 1.3 Vercel 方案

**2026-09-17 決定暫不升級。** 風險要知道：Hobby 方案**禁止商業用途**。只要這個 App 帶來任何商業利益（包含業務員導流），就必須升級到 Pro。這是條款問題，不是效能問題。

### 1.4 法律內容與出險專線定期複查

v58 已全部查證並填上（見 DEVLOG）。法規與專線都會異動，**建議每半年對照一次**：出險專線看汽車交通事故特別補償基金的「各產險公司電話」，法條看全國法規資料庫。**打錯電話、講錯期限的代價由使用者承擔。**

另外強制險的給付金額目前刻意沒寫（2026 年有調整），要補的話同樣要先查證。

### 1.6 App Store 的可行性評估（已決定不上架）

Apple 審查指南 **4.2 Minimum Functionality** 明文排除「把網站包起來」的 App。純 WKWebView 殼被拒是常態。

建議：**先做 Google Play（TWA 是成熟做法），iOS 先靠「加入主畫面」**。等到有原生功能非做不可時（例如背景定位、緊急聯絡人自動通知）再評估 iOS，不要先付 99 美元年費才發現過不了。

---

## 二、產品本身的缺口

### 2.1 對方資料沒有出口（求償的部分已解）

**v54 做了求償這一半**：金額填完之後可以匯出成一張「損害賠償請求明細」，能列印（另存 PDF）也能複製成文字。那張表裡順帶帶上了兩造資料與事故時間地點。

**還沒解的是現場那一半**：對方的車牌、姓名、電話、保險公司，在事故當下要唸給警察、傳給業務員，目前只能自己一個一個唸。求償明細只有在「已經填了金額」之後才產得出來，現場那一刻用不上。

可以做成「現場 → 對方資料卡 → 分享」，內容就是四個欄位加上時間地點。案件概念已於 v27 完成，容器不必另外設計。

### 2.2 照片有遺失風險

照片存在 IndexedDB，會隨「清除瀏覽器資料」一起消失。目前的做法是在 App 內提示使用者用「分享」備份到相簿 —— **這是提示，不是保障**。

沒有雲端備份是刻意的隱私選擇，但至少可以做得更好：例如拍完後主動提示存到相簿、或在儲存空間不足時警告。

### 2.3 行車紀錄器影片

出險流程裡提到「交付照片與行車紀錄器」，但 App 完全沒有處理影片。影片檔案大、處理慢，需要單獨評估。

---

## 三、體驗優化

### 3.1 首次開啟的引導

現在打開就是完整介面。沒出過事故的人不知道這個 App 能做什麼，也就不會在真的出事時想到它。

### 3.2 引導模式（v63 移除）

整個拿掉了，現場頁只剩固定五步。之後如果要依情況改寫步驟，DEVLOG v57／v63 有當時的做法與移除理由。

### 3.3 無障礙（部分完成）

**已做**：對比度全面達到 WCAG AA 4.5:1、觸控面積全部 ≥ 44×44、字級最小 12px。稽核腳本在 `tests/a11y.py`，**v55 起涵蓋 20 個畫面 × 淺深兩色**（五個分頁＋全部十一個覆蓋層），改版後可重跑。

**未做**：螢幕閱讀器（VoiceOver／TalkBack）實機測試、鍵盤操作順序。（`prefers-reduced-motion` 目前只有 v52 歡迎屏那枚脈動的盾有處理，其餘動效還沒盤點。）

### 3.4 登入流程與 App 本體的配色斷層

v59 移除登入流程，不再是問題。

根目錄的 `圖片.png`（2MB）沒有任何引用，確定不用了就可以刪。

---

## 四、商業層面的未解題

### 4.1 帳號制與多租戶架構（v59 取消）

M1–M3 做完但一直預設關閉，v59 決定整個移除：App 直接發給客戶，不需要註冊，所有資料只存在手機裡。設計留在 [ARCHITECTURE.md](ARCHITECTURE.md)。

代價要知道：**換手機或清除瀏覽器資料，事故紀錄與照片都找不回來。** 如果之後客戶反映這是問題，再回頭看 ARCHITECTURE。

### 4.2 誰付錢、付多少 ⭐ 待解

**v43 取消業務員綁定之後，目前沒有任何收入來源。**

原本的模式是 B2B2C：業務員付費訂閱、客戶免費使用，訂閱賣在業務員網頁後台而不走 App 內購，藉此避開 Google Play 15–30% 抽成。**那個模式整個建立在綁定上**，綁定沒了，它就不成立了。

避開抽成的做法本身仍然有效，可以留給未來任何付費設計沿用。要賣什麼、賣給誰，待定。

### 4.3 法律責任邊界

App 提供事故處理指引。若使用者照著做卻產生不利結果，責任如何界定？目前只有免責聲明，**建議在有實際使用者之前先諮詢律師**。

---

## 五、技術債

### 5.1 `index.html` 已經八萬多字元

單檔是刻意的選擇（見 DEVLOG），但已經接近可維護的上限。若要再長，需要在**不引入 build step** 的前提下想辦法拆 —— 例如把 SVG sprite 或 INSURERS 資料抽成獨立檔案由 SW 快取。

### 5.2 沒有自動化測試

驗證腳本收在 `tests/`（見該目錄的 README），但仍是**手動重跑**：改一個地方要跑哪幾支，全憑記憶。

### 5.3 沒有錯誤回報

使用者遇到問題，我們完全不會知道。無後端架構下這是天然的限制，但至少可以做「把錯誤訊息複製給我」這種手動回報。

---

## 優先順序建議

| 順位 | 項目 | 理由 |
|---|---|---|
| 1 | 一、上架前必辦（1.1–1.5） | 多為純執行；1.4 需要法律查證 |
| 3 | 2.1 對方資料的出口 | 資料填了卻拿不出來；案件容器已就緒 |
| 4 | 3.1 首次引導 | 影響留存，但要先有使用者 |
